# The quote form submits leads to Web3Forms directly from the browser.
# The public access key lives in lib/site.ts (web3formsKey) — no env needed.
# To change the receiving inbox, create a new key at https://web3forms.com
# and update site.web3formsKey.
